node_modules
# Keep environment variables out of version control
.env
.env.local

/src/generated/prisma

# build output
/dist

# fichiers uploadés en local (couvertures de formation...) — un vrai stockage
# (Cloudflare R2) remplacera ce dossier local en production, cf. DATA-MODEL.md
/uploads

# testing / coverage
/coverage

# misc
.DS_Store
*.log
*.tsbuildinfo

# skills de référence vendorisés automatiquement par `prisma init` pour les assistants IA —
# documentation externe, pas du code du projet, se périme vite (à re-générer si besoin).
/.agents
/.claude
/.windsurf
skills-lock.json
